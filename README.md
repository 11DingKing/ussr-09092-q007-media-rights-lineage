# 首发影像授权谱系

这个仓库保存首发影像授权谱系的领域约定和可运行基础。接口、规则、持久化适配器与后台作业应分开维护，连接信息只从环境变量读取。

## 开发

使用 Python 3.11 创建虚拟环境后执行 pip install -e .，再运行 python -m unittest discover -s tests。当前代码提供稳定的数据契约和纯函数示例，后续实现需要保持输入校验、错误可读与事务边界清晰。
